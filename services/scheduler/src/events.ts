
import { scheduleEvents } from "./lib/scheduleEvent";

// register when and what event to fire off here

export const registerEvents = async () => {
  // await scheduleEvents("*/1   * * * *", "test", { message: "test" });
}
